[[permission]]
identifier = "allow-get-pending-file-path"
description = "Allow reading the file path passed when the app was launched"
commands.allow = ["get_pending_file_path"]

[[permission]]
identifier = "allow-read-launch-file"
description = "Allow reading an XML or ZIP file chosen by the user"
commands.allow = ["read_launch_file"]
